{
  "dtp": {
  "errors": {
    "notInSystem": [
      "{username}'s adventurer has no DTP record.",
      "No entry for {username}'s adventurer. Try to /initiate first.",
      "I can't find {username}'s adventurer in the ledger."
    ],
    "invalidAmount": [
      "Amount isn't valid. Use a number \u2265 0.",
      "That won't ink. Enter a non-negative amount.",
      "Positive numbers, please."
    ],
    "invalidPrecision": [
      "DTP uses two decimal places. Please round neatly.",
      "Too many decimals for DTP.",
      "Quil prefers cents, not dust. Two decimals."
    ]
  },

  "add": {
    "ok": [
      "OK. {mention}'s adventurer: {name}: **+{amt} DTP** \u2192 **{newDTP} DTP** | {reasonLine}",
      "Deposit recorded: {mention}'s adventurer: {name}: **+{amt} DTP**. Balance **{newDTP} DTP** {reasonLine}",
      "Ledger updated: {mention}'s adventurer: {name} gained **{amt} DTP** (now **{newDTP} DTP**) {reasonLine}"
    ]
  },
  "adjust": {
    "ok": [
      "OK. {mention}'s adventurer: {name}: **{sign}{absAmt} DTP** \u2192 **{newDTP} DTP** {reasonLine}",
      "Adjusted: {mention}'s adventurer: {name}: **{sign}{absAmt} DTP**. Balance **{newDTP} DTP** {reasonLine}",
      "Correction filed: {mention}'s adventurer: {name}: **{sign}{absAmt} DTP** (now **{newDTP} DTP**) {reasonLine}"
    ]
  },
  "set": {
    "ok": [
      "OK. {mention}'s adventurer: {name}: **+{amt} DTP** \u2192 **{newDTP} DTP** {reasonLine}",
      "Balance reset: {mention}'s adventurer: {name}: **{newDTP} DTP** (prev {oldDTP} DTP) {reasonLine}",
      "Exact set recorded: {mention}'s adventurer: {name} \u2192 **{newDTP} DTP** {reasonLine}"
    ]
  },

  "reasonFmt": "_({reason})_"
}
}
